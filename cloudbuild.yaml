steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/backend:$COMMIT_SHA
      - -f
      - backend/Dockerfile
      - backend
images:
  - gcr.io/$PROJECT_ID/backend:$COMMIT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
